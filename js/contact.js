/**
 * contact.js - Обробка контактної форми
 */

function initContactForm() {
  const form = document.querySelector('#contact-form');
  if (!form) return;

  // Форма не обов'язкова для цієї практичної роботи
  console.debug('initContactForm: contact form initialized');
}
